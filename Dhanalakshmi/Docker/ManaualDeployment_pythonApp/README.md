Install python 
import source code
python -m venv myenv -> create virtual environment
myenv\Scripts\activate.ps1  # Windows PowerShell

pip install requirement.txt -> install dependencies

uvicorn main:app --reload-> to run/start the application