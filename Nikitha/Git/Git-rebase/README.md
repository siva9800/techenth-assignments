1. Created feature1 and feature2 files and added data in both files and commit and push to github repository main branch
2. Create feature branch
3. Create feature3 file and data and commit and push file to feature branch and check feature branch logs --> git log --oneline
4. Now checkout to main branch and create feature4 file and add data and commit and push to main branch and now check main branch logs using git log --oneline
5. Now checkout to feature branch and check history 
6. Now git rebase origin feature branch
7. Check the commit history of feature branch after rebase
