
First create one Repo in git hub.Then clone that repo in local.
created two files merge1, merge2 and added for tracking
then git commit two files 
git push to main.
created a new feature branch (feat/tech).
again created two files in feat/tech (merge3, merge4)and add tracking and committed.
the commit flow :
main = merge1, merge2.
by using "git merge feat/tech"
the commit flow looks like :
merge1, merge2, merge3, merge4.

