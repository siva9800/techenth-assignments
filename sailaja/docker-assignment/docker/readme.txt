we pulled a image (nginx) from docker registry = docker pull nginx.
we created container from image = docker run -d -p 3000:80 nginx.
then we check whether webserver which we created a container in the brower = localhost:3000.
we stop the container = docker stop container id



