In this assignment i have created 2 containers(dbn1,dbn2) and checked the communication between them were working as we built it in default bridge network.

After checking the communication they were working and i used the below commands in this assignment.

1. docker run --name dbn1 -d image name.
2. docker exec -it dbn1 /bin/sh
3. ping <IP of respective containers>.
