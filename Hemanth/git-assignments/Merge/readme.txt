First created a Repo in git hub.Then cloned that repo to local.
created two files notes1, notes2 and added for tracking
then git commit two files
git push to main.
created a new feature branch (feat/merge).
again created two files in feat/tech (notes3)and add tracking and committed.
the commit flow :
main = notes1a, notes2b.
by using "git merge feat/merge"
the commit flow looks like :
notes1a, notes2b, notes3c.
