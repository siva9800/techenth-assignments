Docker Commands practice:

1.docker --version
2.docker info
3.docker pull packagename(nginx)-->it downloads the image
3.docker run -d -p 8080:80 nginx -->(run-create and start the image),-d -> detached i.e run in the background
